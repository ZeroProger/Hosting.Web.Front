export async function kickPlayer(id: number) {
	// #TODO запросы на бек
	console.log(`kicked by id ${id}`)
}

export async function banPlayer(id: number) {
	// #TODO запросы на бек
	console.log(`banned by id ${id}`)
}
